---
import { MuxPlayer } from "@mux/mux-player-astro";

interface Props {
  playbackId: string;
  metadata?: {};
}

const { playbackId, metadata } = Astro.props;
---

<MuxPlayer
  playbackId={playbackId}
  metadata={metadata?? {}}
/>
